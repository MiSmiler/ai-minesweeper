{{HEADER}}

棋盘（Legend）：`.`=hidden，`*`=revealed mine（仅 Lost），`F`=flag，`0-8`=revealed 数字。
每行代表一行：第 1 行是 row 0，最后一行是 row {{LAST_ROW_INDEX}}。行内每个字符代表一格：第 1 个字符是 col 0，最后一个是 col {{LAST_COL_INDEX}}。

我该点哪一格？

{{BOARD}}
